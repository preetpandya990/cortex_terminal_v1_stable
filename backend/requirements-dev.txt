# Cortex AI — Development & Test Dependencies
# These are NOT installed in production Docker images.
# Install alongside requirements.txt:
#   pip install -r requirements.txt -r requirements-dev.txt

# ── Testing ────────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
pytest-cov==6.0.0
pytest-timeout==2.3.1
aiosqlite==0.20.0            # SQLite async driver for in-memory test DB
respx==0.21.1                # Mock httpx.AsyncClient responses in unit tests

# ── Type checking ──────────────────────────────────────────────────────────────
mypy==1.13.0
types-redis==4.6.0.20241004

# ── Dependency management ──────────────────────────────────────────────────────
pip-tools==7.4.1             # pip-compile generates hashed lockfile

# ── Code quality ──────────────────────────────────────────────────────────────
ruff==0.8.0                  # Linting + formatting (replaces flake8, isort, black)