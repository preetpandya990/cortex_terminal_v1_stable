# Cortex AI — ML Training Dependencies
# =====================================
# Additional dependencies for model training (not needed for inference)
# Install with: pip install -r requirements-ml-training.txt

# ── ML Training Frameworks ─────────────────────────────────────────────────────
xgboost==2.0.3                  # Gradient boosting (latest stable)
tensorflow==2.15.0              # Deep learning framework
keras==2.15.0                   # High-level neural networks API

# ── Hyperparameter Tuning ──────────────────────────────────────────────────────
optuna==3.5.0                   # Hyperparameter optimization for XGBoost
keras-tuner==1.4.7              # Hyperparameter optimization for Keras

# ── Model Export ───────────────────────────────────────────────────────────────
onnx==1.15.0                    # ONNX format
tf2onnx==1.16.1                 # TensorFlow to ONNX converter
skl2onnx==1.16.0                # Scikit-learn to ONNX (for XGBoost)

# ── Feature Engineering ────────────────────────────────────────────────────────
# Note: We use custom feature engineering in app/ml/features/
# No external TA library needed

# ── Utilities ──────────────────────────────────────────────────────────────────
scikit-learn==1.4.0             # ML utilities (metrics, preprocessing)
scipy==1.12.0                   # Scientific computing
joblib==1.3.2                   # Model serialization

# ── Visualization (Optional) ───────────────────────────────────────────────────
matplotlib==3.8.2               # Plotting
seaborn==0.13.1                 # Statistical visualization
plotly==5.18.0                  # Interactive plots

# ── Progress Tracking ──────────────────────────────────────────────────────────
tqdm==4.66.1                    # Progress bars
